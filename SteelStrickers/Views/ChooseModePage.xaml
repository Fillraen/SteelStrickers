<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:SteelStrickers.ViewModels;assembly=SteelStrickers"
             x:Class="SteelStrickers.Views.ChooseModePage">
    <ContentPage.BindingContext>
        <viewModels:ChooseModeViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid  Grid.Column="0">
                <Button Text="Course" BackgroundColor="Red" Command="{Binding GoToControllerPageCommand}"/>
            </Grid>
            <Grid BackgroundColor="bLUE" Grid.Column="1">
                <StackLayout>
                    <Button Text="Combat" Command="{Binding SearchOpponentCommand}"/>
                    <StackLayout>
                        <Label Text="Rejoindre une partie"/>
                        <Entry Text="{Binding MatchId, Mode=TwoWay}"/>
                    </StackLayout>

                    <Button Text="Create a Match" Command="{Binding CreateMatchCommand}"/>
                </StackLayout>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>